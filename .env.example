# -----------------------------------------------
# PharmaCore Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# -----------------------------------------------

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=pharmacy_npr
DB_USER=root
DB_PASS=

# App (optional — set by installer, can override here)
# APP_TIMEZONE=Asia/Kathmandu
